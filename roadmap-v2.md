# نقشه راه نسخه‌ی ۲ — تصمیمات نهایی + شروع پیاده‌سازی

این نسخه ادامه‌ی `roadmap-desktop-app.md` قبلی است، با ۴ تصمیم نهایی‌شده‌ی زیر. کد اسکلت پروژه هم همراه این فایل تحویل داده شده (پوشه‌ی `server/` و `client/`).

---

## تصمیم ۱: معماری — چندکاربره با دیتابیس مرکزی

چون تایید کردید چند مشاور روی چند سیستم مختلف در دفتر کار می‌کنند، معماری نهایی **۳ لایه** است (نه فقط ۲ لایه‌ی اولیه):

```
[لپ‌تاپ مشاور ۱]  ┐
[لپ‌تاپ مشاور ۲]  ├──HTTP (شبکه‌ی محلی دفتر)──►  [سرور دفتر: FastAPI + PostgreSQL]
[لپ‌تاپ مدیر]      ┘
```

**چرا یک لایه‌ی FastAPI اضافه شد (و کلاینت‌ها مستقیم به PostgreSQL وصل نمی‌شوند):**
- اگر هر لپ‌تاپ مستقیم به PostgreSQL وصل شود، رمز عبور دیتابیس باید روی هر لپ‌تاپ ذخیره شود — دقیقاً همان چیزی که در تصمیم ۳ (امنیت در صورت سرقت) نمی‌خواهیم.
- با FastAPI، فقط سرور دفتر رمز دیتابیس را دارد. کلاینت‌ها فقط با نام‌کاربری/رمزعبور خودشان یک توکن موقت می‌گیرند.
- قانون «مشاور فقط فایل خودش را می‌بیند» یک‌بار در کد سرور نوشته می‌شود و از هر لپ‌تاپی که وصل شود قابل دور زدن نیست (برخلاف حالتی که این قانون فقط در کد کلاینت باشد).
- پیاده‌سازی همین امروز در پوشه‌ی `server/app/api/routes/properties.py` انجام شده (تابع `_base_query`).

**نکته‌ی امنیت شبکه‌ی حیاتی:** روی سرور دفتر، پورت PostgreSQL (۵۴۳۲) را در فایروال ویندوز **به کل شبکه باز نکنید**. فقط FastAPI (پورت ۸۰۰۰) باید از لپ‌تاپ‌های دیگر در دسترس باشد؛ PostgreSQL باید فقط از خودِ همان سرور (`localhost`) در دسترس بماند. این در `server/app/core/config.py` با `POSTGRES_HOST = "localhost"` رعایت شده.

---

## تصمیم ۲: استراتژی پشتیبان‌گیری برای ۱۰٬۰۰۰+ فایل و رشد مداوم

با این حجم داده و رشد آن، توصیه‌ی من رعایت **قانون ۳-۲-۱** است:

- **۳ نسخه** از داده وجود داشته باشد
- روی **۲ نوع رسانه‌ی مختلف** (مثلاً: دیسک اصلی سرور + یک هارد اکسترنال یا فضای ابری)
- **۱ نسخه خارج از دفتر (Offsite)** — اگر آتش‌سوزی یا سرقت کل دفتر رخ دهد، نسخه‌ی داخل همان دفتر هم از بین می‌رود؛ فقط نسخه‌ی Offsite شما را نجات می‌دهد

**چرخش (Rotation) برای کنترل حجم:**
| بازه | نگهداری | برای چه؟ |
|---|---|---|
| روزانه | ۱۴ روز اخیر | بازیابی سریع در صورت اشتباه امروز/دیروز |
| هفتگی | ۸ هفته‌ی اخیر | بازیابی در صورت خطایی که دیرتر متوجه شدید |
| ماهانه | ۱۲ ماه اخیر | آرشیو بلندمدت/قانونی |

**پیاده‌سازی:** اسکریپت `server/scripts/backup.py` همین امروز این منطق را دارد — `pg_dump` فشرده از دیتابیس + zip از پوشه‌ی عکس‌ها + چرخش خودکار. کافی است آن را در Windows Task Scheduler هر شب ساعت ۳ بامداد اجرا کنید (دستور دقیق در `README.md`).

**کاری که خودتان باید انجام دهید (اسکریپت نمی‌تواند خودکار انجامش دهد):** بخش «Offsite» را باید خودتان تأمین کنید، یکی از این دو راه:
1. یک هارد اکسترنال که هفته‌ای یک‌بار از دفتر خارج می‌شود (مثلاً به خانه برده می‌شود)، یا
2. همگام‌سازی خودکار پوشه‌ی بک‌آپ با یک فضای ابری (با ابزار `rclone`) — چون در ایران هستید، گزینه‌های داخلی مثل Object Storage آروان‌کلاود یا لیارا معمولاً از فیلترینگ مصون‌ترند نسبت به سرویس‌های خارجی.

**تست بازیابی:** حداقل هر سه ماه یک‌بار واقعاً یک بک‌آپ را روی یک دیتابیس آزمایشی Restore کنید (`pg_restore`) تا مطمئن شوید فایل بک‌آپ سالم و قابل استفاده است — بک‌آپی که تست نشده، اطمینانی ایجاد نمی‌کند.

---

## تصمیم ۳: مکانیزم آپدیت نرم‌افزار برای همه‌ی مشاوران

چون معماری چندکاربره‌ی شماست و همه‌ی کلاینت‌ها همیشه به یک سرور مرکزی وصل می‌شوند، از همان سرور به‌عنوان منبع نسخه‌ی جدید استفاده می‌کنیم — نیازی به سرویس آپدیت اینترنتی جداگانه (مثل چیزی که اپ‌های تجاری استفاده می‌کنند) نیست:

1. سرور یک endpoint دارد: `GET /version` که نسخه‌ی فعلی + لینک دانلود نصب‌کننده را برمی‌گرداند (پیاده‌سازی: `server/app/api/routes/version.py`).
2. هر بار کلاینت باز می‌شود، این آدرس را چک می‌کند (`client/updater.py`). اگر نسخه فرق داشت، به کاربر پیام می‌دهد و دکمه‌ی «دانلود نسخه‌ی جدید» را نشان می‌دهد که مستقیم فایل exe جدید را از همان سرور دفتر دانلود می‌کند.
3. برای انتشار نسخه‌ی جدید، شما فقط باید:
   - فایل `exe` جدید (ساخته‌شده با PyInstaller) را در `server/static_installers/` کپی کنید
   - `APP_VERSION` را در فایل `.env` سرور آپدیت کنید و سرور را ری‌استارت کنید
   - دیگر نیازی نیست دستی سراغ لپ‌تاپ هر مشاور بروید.

این ساده‌ترین و مطمئن‌ترین روش برای محیط شبکه‌ی محلی دفتر است (بدون وابستگی به اینترنت یا سرویس‌های آپدیت شخص ثالث).

---

## تصمیم ۴: امنیت دیتابیس در صورت گم‌شدن/سرقت لپ‌تاپ

خبر خوب: با معماری چندکاربره‌ای که انتخاب کردید (تصمیم ۱)، **این ریسک به‌طور طبیعی بسیار کم شده**، چون:

- دیتابیس اصلاً روی لپ‌تاپ مشاور نیست؛ فقط روی سرور مرکزی دفتر است.
- لپ‌تاپ مشاور فقط یک برنامه‌ی کلاینت دارد که هیچ رمز دیتابیسی در خودش ندارد (`client/config.py` فقط آدرس سرور را دارد، نه رمز).
- توکن ورود کاربر فقط در حافظه‌ی برنامه (RAM) نگه داشته می‌شود، نه روی دیسک؛ با بستن برنامه از بین می‌رود.
- اگر لپ‌تاپ گم/دزدیده شود، همان لحظه که متوجه شدید، کافی است از پنل مدیریت کاربران آن حساب را «غیرفعال» کنید یا رمزش را ریست کنید — چون در کد (`token_version`) طوری طراحی شده که این کار فوراً همه‌ی نشست‌های باز آن کاربر را باطل می‌کند، حتی اگر لپ‌تاپ روشن و برنامه باز مانده باشد.

**چیزهایی که باید خودتان تأمین کنید (چون خارج از کنترل کد است):**
1. **امنیت فیزیکی خودِ سرور دفتر** حالا از همه مهم‌تر است (چون همه‌ی داده آن‌جاست): رمزنگاری کامل دیسک با BitLocker، قرار دادن سرور در جایی با دسترسی فیزیکی محدود، رمز عبور ویندوزی قوی.
2. **رمز عبور قوی روی هر حساب مشاور** + آموزش این‌که رمز را روی خود لپ‌تاپ ذخیره/یادداشت نکنند.
3. **قفل خودکار صفحه‌ی لپ‌تاپ** بعد از چند دقیقه بی‌کاری (تنظیمات ویندوز) تا کسی که لپ‌تاپ باز و روشن را بردارد، حداقل نتواند مستقیم وارد ویندوز شود.
4. اگر بعداً تصمیم گرفتید داده‌ای را موقتاً روی خودِ لپ‌تاپ کش (cache) کنید برای سرعت بیشتر، حتماً آن را رمزنگاری‌شده ذخیره کنید، نه متن ساده — فعلاً در این نسخه‌ی اسکلت هیچ کشی روی دیسک کلاینت انجام نمی‌شود.

---

## نکته‌ی اضافه: راست‌به‌چپ و فارسی‌سازی

- کل رابط گرافیکی با `app.setLayoutDirection(Qt.RightToLeft)` راست‌به‌چپ شده (`client/ui/styles.py`).
- فونت پیشنهادی: **Vazirmatn** (رایگان، متن‌باز، خوانایی خوب برای فارسی) — باید فایل ttf را دانلود و در `client/resources/fonts/` قرار دهید (لینک در `README.md`).
- **نکته‌ی مهمی که هنوز پیاده نشده و باید در فاز فرم‌ها اضافه شود:** چون این اپ برای بازار املاک ایران است، تاریخ‌های قرارداد (پایان اجاره/رهن) باید با **تقویم شمسی** نمایش داده شوند، نه میلادی. کتابخانه‌ی `jdatetime` را از همین حالا در `requirements.txt` هر دو بخش (سرور و کلاینت) اضافه کرده‌ام؛ در فاز ساخت فرم‌های افزودن/ویرایش فایل، باید یک ویجت انتخاب تاریخ شمسی (Date Picker) بسازید یا یک کتابخانه‌ی آماده‌ی PySide6 برای آن پیدا کنید — این یکی از اولین کارهای فاز بعدی است.
- اعداد: اگر می‌خواهید ارقام فارسی (۱۲۳۴) به‌جای انگلیسی (1234) نمایش داده شوند، این فقط در لایه‌ی نمایش (UI) انجام شود؛ در دیتابیس همیشه عدد استاندارد ذخیره بماند تا محاسبات و فیلترها خراب نشوند.

---

## آنچه همین الان تحویل داده شده (کد واقعی، نه فقط طرح)

پوشه‌ی `realestate-app/` شامل یک اسکلت کاملاً کار‌کننده است:

- **سرور (FastAPI):** مدل‌های User/Property/ActivityLog، احراز هویت JWT با قابلیت ابطال فوری توکن، فیلتر دسترسی مشاور/مدیر روی هر endpoint، endpoint نسخه برای مکانیزم آپدیت، اسکریپت بک‌آپ با چرخش، اسکریپت ساخت اولین مدیر.
- **کلاینت (PySide6):** پنجره‌ی ورود، پنجره‌ی اصلی با تب فهرست فایل‌ها/قراردادهای رو‌به‌اتمام/مدیریت کاربران (تب آخر فقط برای مدیر)، راست‌به‌چپ کامل، چک خودکار نسخه‌ی جدید هنگام باز شدن.
- **مستندات اجرا:** `README.md` با دستورهای دقیق ساخت venv (سرور و کلاینت جدا از هم)، نصب کتابخانه‌ها، تنظیم PostgreSQL، اجرای سرور و کلاینت.

## قدم بعدی (فاز ۳ طبق نقشه‌ی قبلی)

پیشنهاد من برای قدم بعدی: بعد از اینکه سرور و کلاینت را روی سیستم خودتان بالا آوردید و توانستید با کاربر مدیر وارد شوید و فهرست خالی را دیدید، برویم سراغ ساخت **فرم گرافیکی کامل افزودن/ویرایش فایل ملکی** (با ۴ نوع معامله و فیلدهای اختصاصی هرکدام + تاریخ شمسی)، چون این چیزی است که هر روز بیشترین استفاده را دارد.
