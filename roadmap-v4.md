
## بخش الف — تعهدات roadmap-v3: همگی انجام شد ✅

| # | مورد v3 | وضعیت |
|---|---|---|
| ۱ | صفحه‌بندی واقعی (حذف حد ۵۰۰) | ✅ page/page_size/total + نوار صفحه‌بندی |
| ۲ | آرشیو و بازگردانی | ✅ بایگانی سه‌گانه + بازگردانی + انقضا با تأیید مالک |
| ۳ | ActivityLog واقعی | ✅ ثبت در تمام عملیات + تب تاریخچه صفحه‌بندی‌شده |
| ۴ | جستجوی آزاد | ✅ زندهٔ چندستونه با هایلایت + Ctrl+F |
| ۵ | مرتب‌سازی | ✅ sort_by سرور + مرتب‌سازی کارت‌ها |
| ۶ | تنظیمات سرور خارجی | ✅ settings.json + پنجرهٔ «تنظیمات اتصال» |
| ۷ | انقضای نشست روشن | ✅ هندلر مرکزی ۴۰۱ |
| ۸ | قفل هم‌زمان | ✅ version + ۴۰۹ |
| ۹ | تکراری ایمپورت | ✅ هشدار مشابه (فرم + ایمپورت) |
| ۱۰ | لاگ فایل | ✅ setup_logging |
| ۱۱ | هشدار فوریت ورود | ✅ دیالوگ خودکار زیر-۷-روز |
| ۱۲ | بستن /docs | ✅ DEBUG=false |

## بخش الف۲ — زیرساخت ابری (Runflare) ✅

- سرویس API (Docker، gunicorn + UvicornWorker) + PostgreSQL 16 مدیریتی جدا
- envها روی پنل؛ postStart = alembic + ensure_columns
- میکرو-مهاجرت idempotent: ستون‌های جدید، مقادیر جدید Enum، تبدیل پول‌ها به NUMERIC
- دیسک پایدار: media (عکس/رسید/بکاپ) • static_installers • logs
- دیتابیس به اینترنت باز نمی‌شود؛ فقط API
- آینهٔ PyPI (Tsinghua) در Dockerfile — PyPI مستقیم از شبکهٔ ایران در دسترس نیست

## بخش ب — ۲۰ ویژگی v1.4 ✅ (فهرست کامل با جزئیات فنی)

۱. **قیمت/متری در جدول** — سرور `price_display` و `price_per_m2_display` می‌سازد (قالب نوع-محور)؛ جدول و کارت از یک منبع
۲. **بج قیمت کارت** — یک منبع سرور، قالب درست اجاره («ودیعه X / اجاره Y»)
۳. **بج رنگی وضعیت معامله** — سبز/زرد/قرمز
۴. **Toast** — ۱.۵ ثانیه + فید؛ مودال فقط خطا/تأیید
۵. **اسپینر جداول** — overlay شفاف با hide/show
۶. **Empty-state** — اطلاعیه‌ها 📭، نمودار داشبورد
۷. **تماس/واتساپ/نقشه** — منوی کانتکست از ردیف فایل + دکمه روی کارت
۸. **فوریت** — `urgent_until` (Date) + بج 🔥 + فیلتر سروری `urgent_only` (فوری یا قرارداد ≤۷ روز)
۹. **بج عکس + دکمهٔ تماس کارت** — کپی یک‌کلیکی
۱۰. **location_url** — فیلد فرم + دکمهٔ «🗺 مکان»
۱۱. **ماشین‌حساب رهن↔اجاره** — ضریب قابل ویرایش در پنجره (پیش‌فرض ۳۰، ذخیره در settings.json)
۱۲. **اطلاع‌یهٔ تطبیق خودکار** — POST /properties/notify-matches/{id}؛ منطق تطبیق مشترک با request_matches
۱۳. **Ctrl+K سراسری** — فایل‌ها (سرور search) + درخواست‌ها (فیلتر کلاینت)؛ Enter باز می‌کند
۱۴. **اکسل معامله‌ها** — /deals/export/excel (openpyxl، RTL)
۱۵. **Pipeline مشتری** — Enum جدید (open/contacted/visited/negotiation/won/lost) + کمبوی ثبت مرحله + شمارندهٔ داشبورد
۱۶. **فایل‌های کهنه** — GET /properties/stale (فعالِ بدون پیگیری N روز) + دیالوگ با دابل‌کلیک
۱۷. **rented** — Enum جدید PropertyStatus؛ قطعی اجاره/رهن → rented (نه sold)؛ unfinalize هر دو را برمی‌گرداند
۱۸. **بکاپ** — سرویس JSON gzip (۱۲ جدول) + cron ۰۲:۳۰ + /backups/run|list|download + UI مدیر
۱۹. **آواتار چت** — رنگ ثابت از نام فرستنده
۲۰. **Session مشترک HTTP** — requests.Session؛ TLS یک‌بار handshake (رفع کندی ~۳ثانیه/درخواست)

## بخش ج — اصلاحات مهم همین دوره ✅

- **حذف سقف مبالغ**: BigInteger → NUMERIC (سقف عملی حذف؛ ثبت ۲۶ رقم تست شد)
- **رفع کرش ۵۰۰**: ستون‌های مفقود مدل (commission_percent, kind) — میکرو-مهاجرت
- **رفع چرخهٔ ایمپورت** properties↔client_requests (ایمپورت محلی)
- **RTL ساعت و فشرده‌سازی تاریخ شمسی** (حذف min-width سراسری اسپین‌باکس)
- **آپلود تکه‌تکهٔ نصب‌کننده** /installers/chunk + /finalize (۵ تکه، retry)
- **اتصال مشاور جدید به چت**، عرض سایدبار با فونت، فیکس سیگنال currentCellChanged
- **فونت نمودارها** مقیاس با فونت کاربر
- **رفرش کارت‌ها بعد از ذخیره** (_reload_all)

## بخش د — آموخته‌های عملیاتی 📚

- CrashLoopBackOff در K8s = postStart شکست خورده یا دستور اصلی تمام‌شونده — لاگ را ببین
- رمز دیتابیس را **هرگز** از اسکرین‌شات تایپ نکن؛ از دکمهٔ کپی URI
- متغیر env فقط به پادِ تازه می‌رسد؛ بعد از تغییر env حتماً دیپلوی
- smart screen ویندوز: exe بدون امضا «ناشناس» است — More info → Run anyway
- کندی ادراک‌شده را اول با curl -w اندازه بگیر؛ TLS handshake ≠ کندی اپ

## بخش هـ — صف v1.5+ (پیشنهادی)

- [ ] env رندر نرم‌افزاری در ریلیز بعد (فیکس سفید/فریز برخی GPUها)
- [ ] «در حال تایپ» واقعی (POST /chat/typing + TTL)
- [ ] ری‌استور بکاپ JSON از UI
- [ ] پاک‌سازی خودکار activity_logs/chat_messages (Storage فعلاً ۳۸٪ — فوری نیست)
- [ ] فیلتر rented در UI بایگانی (endpoint آماده؛ UI مانده)
- [ ] Code Signing در صورت توزیع عمومی‌تر
- [ ] اطلاع‌یهٔ تلگرام (در صورت نیاز آینده)
```