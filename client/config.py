"""
تنظیمات کلاینت — نکته‌ی امنیتی: این فایل هیچ رمز عبور یا اطلاعات حساسی ندارد،
چون دیتابیس اصلاً روی لپ‌تاپ مشاور نیست.

نکته‌ی مهم: SERVER_URL این‌جا فقط «مقدار پیش‌فرض اولیه» است — یعنی وقتی برنامه
برای اولین بار روی یک سیستم اجرا می‌شود و هنوز settings.json کنار exe ساخته
نشده. آدرس واقعی سروری که برنامه در عمل استفاده می‌کند از settings_manager.py
(فایل settings.json کنار exe، یا از پنجره‌ی «تنظیمات اتصال» در ورود) خوانده
می‌شود. اگر IP سرور دفتر عوض شد، دیگر نیازی به تغییر این فایل و ساخت دوباره‌ی
exe نیست — کافی است settings.json یا همان پنجره‌ی تنظیمات را ویرایش کنید.
"""

# مقدار پیش‌فرض اولیه (فقط برای اولین اجرا، قبل از ساخته‌شدن settings.json)
SERVER_URL = "http://192.168.1.10:8000"

APP_VERSION = "1.0.0"
