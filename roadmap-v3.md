# نقشه راه نسخه‌ی ۳ — از یک نمونه‌ی کارکرد به یک اپلیکیشن واقعی و آماده‌ی تولید

roadmap-v2.md تمام شد. این نسخه یک تحلیل صادقانه است از چیزهایی که یک اپلیکیشن
**واقعی، در مقیاس ۱۰٬۰۰۰+ فایل، با چند کاربر هم‌زمان** لازم دارد ولی الان یا
اصلاً نداریم، یا نسخه‌ی خیلی ساده‌شده‌اش را داریم. اولویت‌بندی بر اساس «چقدر
بدون این چیز، اپلیکیشن در استفاده‌ی واقعی مشکل پیدا می‌کند» است.

---

## ۱. صفحه‌بندی واقعی فهرست فایل‌ها (بحرانی — همین الان شروع می‌کنیم)

**مشکل الان:** `list_properties` یک `.limit(500)` هاردکد دارد. یعنی اگر مدیر
بخواهد همه‌ی فایل‌های دفتر را ببیند و تعدادشان از ۵۰۰ بیشتر شود (که با ۱۰٬۰۰۰+
فایل قطعی است)، فایل‌های ۵۰۱ به بعد **اصلاً دیده نمی‌شوند** — نه خطایی، نه
پیامی؛ فقط بی‌صدا از دید مدیر پنهان می‌مانند. این دقیقاً همان مقیاسی است که
خودتان از ابتدا گفتید.

**راه‌حل:** صفحه‌بندی واقعی (page/page_size) + برگرداندن تعداد کل نتایج، تا
کلاینت بتواند «صفحه ۱ از ۲۰ — تعداد کل: ۹۸۷۶» نشان دهد و دکمه‌ی صفحه‌ی بعد/قبل
داشته باشد.

---

## ۲. آرشیو و بازگردانی فایل‌های غیرفعال‌شده

**مشکل الان:** دکمه‌ی «غیرفعال کردن فایل» هست، ولی هیچ راهی برای دیدن فایل‌های
غیرفعال‌شده یا برگرداندن‌شان به حالت فعال نیست (برخلاف کاربران که دکمه‌ی
فعال‌سازی دارند). اگر مشاوری اشتباهی فایلی را غیرفعال کند، آن فایل عملاً از
دسترس خارج می‌شود — فقط مدیر پایگاه‌داده می‌تواند برش گرداند.

## ۳. ثبت واقعی تاریخچه‌ی تغییرات (ActivityLog)

**مشکل الان:** مدل `ActivityLog` از فاز اول ساخته شده ولی **هیچ‌جای کد چیزی
در آن ثبت نمی‌شود** و هیچ صفحه‌ای هم برای دیدنش نیست. یعنی اگر مدیر بپرسد «این
فایل رو کی و کی تغییر داده؟» یا «فلان مشاور چند فایل امروز اضافه کرده؟»،
هیچ پاسخی نیست — درحالی‌که این دقیقاً کاربردی است که یک مدیر واقعی نیاز دارد.

## ۴. جستجوی آزاد (کلیدواژه)

فیلترهای فعلی همه ساختاریافته‌اند (شهر، متراژ، قیمت...) ولی یک جعبه‌ی جستجوی
ساده که در آدرس/توضیحات/نام مالک هم‌زمان بگردد نداریم — چیزی که هر اپلیکیشن
مدیریت داده‌ی امروزی دارد («فقط بنویس و پیدا کن»).

## ۵. مرتب‌سازی فهرست

فهرست همیشه بر اساس تاریخ ثبت مرتب می‌شود. امکان مرتب‌سازی بر اساس قیمت،
متراژ، یا نزدیک‌ترین تاریخ پایان قرارداد وجود ندارد.

## ۶. تنظیمات سرور در فایل خارجی قابل‌ویرایش

**مشکل الان:** آدرس سرور (`SERVER_URL`) داخل `client/config.py` هاردکد است.
یعنی اگر IP سرور دفتر عوض شود، باید کد را عوض کنید و دوباره exe بسازید و آن را
روی همه‌ی لپ‌تاپ‌ها نصب کنید. یک اپلیکیشن واقعی این تنظیم را در یک فایل ساده
(مثلاً کنار exe یا در پوشه‌ی کاربر) نگه می‌دارد تا خودِ مدیر دفتر بدون دانستن
برنامه‌نویسی بتواند تغییرش دهد.

## ۷. مدیریت خودکار انقضای نشست (Session Expiry)

توکن بعد از ۸ ساعت منقضی می‌شود. الان اگر این اتفاق بیفتد، کاربر فقط یک پیام
خطای گنگ می‌بیند. باید به‌طور خودکار او را به صفحه‌ی ورود برگرداند با پیام
روشن «نشست شما پایان یافته، دوباره وارد شوید».

## ۸. قفل هم‌زمان (Optimistic Locking)

این نگرانی از roadmap-v1 مطرح شده بود ولی هنوز پیاده نشده: اگر دو مشاور
هم‌زمان یک فایل را ویرایش کنند، آخرین ذخیره بی‌سروصدا تغییرات دیگری را
رونویسی می‌کند، بدون هیچ هشداری.

## ۹. جلوگیری از ثبت تکراری در Import اکسل

اگر یک فایل اکسل دوبار Import شود (مثلاً به اشتباه)، همه‌ی ردیف‌ها دوباره به
عنوان فایل‌های جدید ثبت می‌شوند — نه هشداری، نه تشخیص تکراری.

## ۱۰. لاگ فایل ساختاریافته روی سرور

الان فقط خروجی uvicorn روی صفحه‌ی ترمینال است. اگر سرور شب/آخر هفته کرش کند و
کسی پای سیستم نباشد، هیچ لاگی برای بررسی بعدی نمی‌ماند.

## ۱۱. اعلان فوریت‌های قرارداد هنگام ورود

تب «قراردادهای رو‌به‌اتمام» هست ولی کاملاً منفعل است — کاربر باید خودش بازش
کند. یک اپلیکیشن مدرن معمولاً هنگام ورود اگر قراردادی خیلی نزدیک به پایان
باشد (مثلاً کمتر از ۷ روز)، یک پیام برجسته نشان می‌دهد.

## ۱۲. محدودسازی مستندات عمومی API (`/docs`)

مستندات خودکار Swagger UI فعلاً برای هرکسی روی شبکه‌ی دفتر بدون نیاز به لاگین
قابل مشاهده است (فقط ساختار API، نه داده — ولی بازهم بهتر است در محیط عملیاتی
غیرفعال یا محدود شود).

---

## ترتیب پیاده‌سازی

از مورد ۱ (صفحه‌بندی) شروع می‌کنیم چون مستقیماً به مقیاس واقعی شما (۱۰٬۰۰۰+ فایل)
مربوط است و بدون آن ممکن است الان هم داده‌ای بی‌صدا از دید مدیر پنهان مانده باشد.
